
# Advantage 360 Pro ZMK アップグレードガイド

最後のV2.0アップデートと新しいV3.0ブランチでは、カスタムキーマップを使用しているユーザーが更新する際にマージコンフリクトを引き起こす変更がキーマップファイルに導入されました。このガイドでは、何が、なぜ変更されたのか、そしてそれをどのように修正するかについて説明します。

## 変更点とその理由

一部のユーザーが遭遇していたマトリックス外のキープレスの問題を解決し、今後のすべてのアップデートでキーマップを効率化するため、使用されていないいくつかのキーがキーマップと[マトリックス変換](https://zmk.dev/docs/config/kscan#matrix-transform)から削除されました。また、フォーマットもZMKのフォーマット規約に沿うように標準化されました。その結果、ZMKメインへのマージを目指している[こちら](https://github.com/zmkfirmware/zmk/pull/1454)のAdvantage 360 Proのボード定義は、このリポジトリの定義と最大限一致するものとなっています。
これらの変更により、更新手順で2つの異なるgitブランチを統合しようとするとマージコンフリクトが発生します。
この種の破壊的変更は、将来的には再び起こらないはずです。

## マージコンフリクトの解決方法

マージコンフリクトを解決するためには、2つのファイルからコンフリクトしているキービヘイビアを手動で削除するだけです。

* adv360.keymap

![削除する必要がある、ハイライトされた&noneビヘイビア](/assets/keymap.jpg)

* keymap.json

![削除する必要がある、ハイライトされた&noneビヘイビア](/assets/json.jpg)

これらハイライトされたキーは、両方のファイルの各レイヤーから削除する必要があります。`keymap.json`では各キービヘイビアの間にカンマを、`adv360.keymap`では各ビヘイビアの間に少なくとも1つのスペースを維持するように注意してください。そうしないとファームウェアのビルドが失敗します。`keymap.json`では、ビヘイビアが正しく引用符で囲まれている必要があります。各ビヘイビアは引用符で囲む必要があります。引用符内の空白は無視されます。
GitHubのWebインターフェースを使ってフォークを更新しようとすると、「このブランチにはマージコンフリクトがあり、自動的にマージできません」というエラーが表示され、代わりにプルリクエストを開くように促されます。この方法でマージコンフリクトを解決するのは、GitHub Desktopで行うよりも困難です。

### Github Desktop

この手順は、[GitHub Desktop](https://desktop.github.com/)をダウンロード、インストールし、あなたのアカウントのGitHub認証情報でログイン済みであることを前提としています。VSCodeのような別のテキストエディタをインストールしておくと便利ですが、この作業はメモ帳でも完了できます。

1. リポジトリをクローンする

        メニューで File->Clone repository に移動します。あなたのAdv360-Pro-ZMKリポジトリのフォークが、選択可能なリポジトリのメニューに表示されるはずです。青い「Clone」ボタンをクリックします。
2. フォルダを開く

    「Show in your file manager」オプションを使ってフォルダを開き、「config」フォルダに移動します。
3. keymap.jsonを編集する

    `keymap.json`を開き、各レイヤーでハイライトされた8つの`&none`ビヘイビアを、それらを囲む引用符とカンマも含めて削除します。
4. adv360.keymapを編集する

    `adv360.keymap`を開き、各レイヤーでハイライトされた8つの`&none`ビヘイビアを削除します。
5. 変更をコミットする

    GitHub Desktopに戻ると、左側のバーに行った変更が表示されます。適切なコミット概要を入力し、「Commit to *branch name*」をクリックします。

    ![GitHub Desktopのコミットダイアログ](/assets/commit.jpg)

6. マージする

    Branch->Merge into current branch に移動し、「upstream/V2.0」または「upstream/V3.0」を選択します。2つのファイルがコンフリクトするという警告が表示されます。「Create a merge commit」をクリックします。

    ![コンフリクトの警告が表示されているマージダイアログ](/assets/merge.jpg)

7. コンフリクトを解決する

    コンフリクト解決ウィンドウには、マージコンフリクトを解決するための強力なオプションがあります。「Open in *your text editor*」の隣にある下矢印をクリックし、「use the modified file from *your branch name*」をクリックします。この例では、ブランチ名はV2.0-betaです。upstream/V2.0やupstream/V3.0のファイルを使用すると、あなたのキーマップの変更が一切保持されなくなります。
    この操作を両方のファイルで行うと、「Continue Merge」オプションをクリックできるようになります。

    ![コンフリクトの警告が表示されているマージダイアログ](/assets/conflict.jpg)
8. プッシュする

    マージが完了したら、トップバーの「push origin」ボタンをクリックできるはずです。この時点で、変更がGitHubサーバーにアップロードされ、ビルドが開始されるはずです。
9. ファームウェアをテストする

    ビルドが成功したら、uf2ファイルをダウンロードし、通常通り書き込みます。キーマップをテストし、キーがどちらかにずれている場合は、ビヘイビアを削除しすぎたか、削除が不十分だった可能性があります。
